import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const invoiceFormSlice = createSlice({
  name: "invoiceForm",

  initialState,
  reducers: {},
});

export const {} = invoiceFormSlice.actions;
export default invoiceFormSlice.reducer;
